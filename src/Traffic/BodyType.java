package Traffic;

public enum BodyType {
    CARGO_PASSENGER_BODY;
    enum refrigerator
    {
        MANUFACTURED_GOODS,
        PERISHABLE_PRODUCE
    }
    enum tent{
        MANUFACTURED_GOODS
    }
    enum cistern{
        liquid
    }
}
